The provided file content does not contain an `analyzer.analyze(...)` call or the `file_contents` keyword, so I cannot apply the requested change. Please provide the complete `src/patchly/agent.py` content.